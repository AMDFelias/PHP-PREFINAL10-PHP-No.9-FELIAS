<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Faculty Management System (CRUD)</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        fieldset { border: 1px solid #ccc; padding: 15px; margin-bottom: 20px; }
        label { display: inline-block; width: 130px; font-weight: bold; }
        .form-group { margin-bottom: 12px; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        table, th, td { border: 1px solid #ccc; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .alert { padding: 10px; margin-bottom: 15px; font-weight: bold; border-radius: 4px; }
        .alert-success { background-color: #d4edda; color: #155724; border: 1px solid #c3e6cb; }
        .alert-error { background-color: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; }
        .btn { padding: 6px 12px; text-decoration: none; border-radius: 3px; cursor: pointer; display: inline-block; }
        .btn-primary { background: #007bff; color: #fff; border: none; }
        .btn-secondary { background: #6c757d; color: #fff; border: none; }
        .btn-warning { background: #ffc107; color: #000; text-decoration: none; padding: 4px 8px; font-size: 12px; }
        .btn-danger { background: #dc3545; color: #fff; text-decoration: none; padding: 4px 8px; font-size: 12px; }
    </style>
</head>
<body>